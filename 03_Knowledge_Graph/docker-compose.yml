# 新能源汽车知识图谱 - Neo4j Docker配置
services:
  neo4j:
    image: neo4j:5.14.0-community
    container_name: ev_knowledge_graph_neo4j
    restart: unless-stopped
    ports:
      - "7475:7474"  # Neo4j Browser (使用7475避免端口冲突)
      - "7688:7687"  # Bolt协议 (使用7688避免端口冲突)
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
      - neo4j_import:/var/lib/neo4j/import
      - neo4j_plugins:/plugins
    environment:
      # 认证配置
      - NEO4J_AUTH=neo4j/neo4j123
      
      # 内存配置
      - NEO4J_dbms_memory_heap_initial__size=2G
      - NEO4J_dbms_memory_heap_max__size=4G
      - NEO4J_dbms_memory_pagecache_size=2G
      
      # 基本配置
      - NEO4J_dbms_default__database=neo4j
      - NEO4J_dbms_allow__upgrade=true
      
      # 日志配置 (Neo4j 5.x兼容)
      - NEO4J_dbms_logs_query_enabled=INFO
      - NEO4J_dbms_logs_query_threshold=1s
      
      # 安全配置
      - NEO4J_dbms_security_procedures_unrestricted=gds.*,apoc.*
      - NEO4J_dbms_security_procedures_allowlist=gds.*,apoc.*
      
      # 事务配置
      - NEO4J_dbms_transaction_timeout=30s
      - NEO4J_dbms_transaction_concurrent_maximum=1000

    healthcheck:
      test: ["CMD", "cypher-shell", "-u", "neo4j", "-p", "neo4j123", "RETURN 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

volumes:
  neo4j_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data/neo4j_data
  neo4j_logs:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./logs/neo4j_logs
  neo4j_import:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data/neo4j_import
  neo4j_plugins:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data/neo4j_plugins

networks:
  default:
    name: ev_knowledge_graph_network